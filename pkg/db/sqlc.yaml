version: "2"
sql:

  - queries:
      - "account.sql"
      - "subscription.sql"
    engine: "postgresql"
    schema: "../migration/"
    gen:
      go:
        sql_package: "pgx/v4"
        package: "db"
        out: "."

