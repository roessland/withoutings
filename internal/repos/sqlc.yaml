version: "2"
sql:

  - queries:
      - "db/account.sql"
      - "db/session.sql"
      - "db/subscription.sql"
    engine: "postgresql"
    schema: "../migrations/"
    gen:
      go:
        sql_package: "pgx/v4"
        package: "db"
        out: "db"

